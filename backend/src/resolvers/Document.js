const Document = {
  //parent is Document
  // passBy: async (parent, args, { db }, info)=> {
  //   return Promise.all(parent.passBy.map((Id) => db.UserModel.findById(Id)));
  // },

};
export default Document;
